{
  "conditions": [
    {
      "evaluator": {
        "params": [
          ${expression_to},
          ${expression_from}
        ],
        "type": "${expression}"
      }
    }
  ],
  "expression": "${refId}",
  "refId": "threshold",
  "type": "threshold"
}
